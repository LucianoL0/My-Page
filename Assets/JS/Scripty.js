const firstLetter = document.querySelectorAll('.header-sc1');
const colorH = getComputedStyle(document.body);

document.addEventListener('click', e => {
    const el = e.target;
    if (el.classList.contains('dark-buttom')) {
        
    }
});
